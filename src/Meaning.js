import React from "react";

export default function Meaning(props) {
    if (props.results) {
          return <div className="Meaning">
            {results.meanings[0].map(function (meanings, index))}
          </div>;
    }

  
}
